using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

using Ingame = Sandbox.ModAPI.Ingame;
using VRage.ModAPI;
using VRage.Game.ModAPI;


namespace SEGarden.Extensions {

    using Logging;

    public static class CockpitExtensions {
        /*
        private static Logger Log = new Logger("SEGarden.Extensions.CockpitExtensions");


        public static IMyCharacter GetPilot(this Ingame.IMyCockpit cockpit) {
            var children = new List<IMyEntity>();
            cockpit.GetChildren(children, (e) => e is IMyCharacter);

            if (children.Count > 0) {
                return children.First() as IMyCharacter;
            }

            return null;          
        }
        */
    }
    
}
